---
version: "3.8"
services:
  phy-cluster-ui:
    ports:
      - "8080:8080"
    volumes:
      - "../:/app/"

  phy-cluster-docs:
    entrypoint: "mkdocs serve -v -a 0.0.0.0:8001"
    ports:
      - "8001:8001"
    volumes:
      - "../:/app/"
    image: "phy-cluster-ui"
    container_name: "phy-cluster-docs"
    hostname: "phy-cluster-docs"
    healthcheck:
      disable: true
    tty: true

    depends_on:
      phy-cluster-ui:
        condition: "service_started"

volumes:
  app_data: {}
  doc_data: {}

